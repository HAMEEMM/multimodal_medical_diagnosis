Update project structure and documentation

This commit:
1. Updates project organization with proper directory structure
2. Adds .gitignore configuration to handle large files appropriately
3. Adds .gitkeep files to maintain empty directories in Git
4. Updates README.md with repository setup information
5. Adds SETUP_GUIDE.md with development environment instructions
6. Includes notebook files for medical diagnosis ML implementation
7. Adds test_environment.py for environment verification

The audio files from recordings are not included in git to keep the repository lightweight.
Data can be downloaded using the download_medical_speech_dataset.py script.
